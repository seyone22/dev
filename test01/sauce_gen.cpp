//Sauce generator created by A-kun
//If you know what the codes are for, then you know.
#include <iostream>

using namespace std;
void inputSelector(int& i)
{
    int i = 0;
    cin >> i;
}
int main()
{
    cout << "Welcome to the Sauce Generator! What can we do for you? \n";
    cout << "\t1.\tGenerate a Sauce\n";
    cout << "\t2.\tExit the program\n";
    int i = 0;
    inputSelector(i);
    if (inputSelector(i) == 1)
    {
        cout << "You have requested a code.";
    }
    else if (n == 2)
    {
        exit(0);
    }
    else
    {
    };

    cout << n;
    cout << endl;
    system("pause");
    return 0;
}

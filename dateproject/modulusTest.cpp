#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    /*double k, n;
    do
    {
        cin >> n;
        k = fmod(n, 29.5306);
        if (k < 1)
            cout << "Is a full moon day.";
        else
            cout << "Is not a full moon day";
        cout << k;
    } while (true);*/

    int a, b; 
    do
    {
        cin >> a;
        cout << a / 4 << endl;
    } while (true);
    

    return 0;
}
